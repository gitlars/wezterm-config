-- Copy to local.lua for machine-specific overrides (gitignored).
local wezterm = require 'wezterm'
local M = {}

function M.apply(config)
  -- config.font_size = 14.0
  -- config.color_scheme = 'Tokyo Night Storm'
  -- config.default_prog = { '/opt/homebrew/bin/fish', '-l' }

  -- Windows: PowerShell 7 must be installed separately; stock Windows only
  -- has powershell.exe (5.1).
  -- config.default_prog = { 'pwsh.exe', '-NoLogo' }

  -- Point the LEADER+s project picker at your own directories. Lua caches
  -- modules, so assigning here replaces the defaults in modules/sessions.lua
  -- without editing a tracked file.
  -- local sessions = require 'modules.sessions'
  -- sessions.roots = {
  --   wezterm.home_dir .. '/repos',
  --   wezterm.home_dir .. '/clients',
  -- }
end

return M
